---
id: argument-storage-not-stable-kind
type: argument
title: Pure storage is not a stable kind at mind-matching scale
author: mishka
status: asserted
tags: [philosophy-of-mind, functionalism, computability]
premise: [claim-lookup-table-physically-unrealisable, claim-lookup-table-size-exponential]
concludes: claim-realisable-behaviour-matcher-is-organised
attacks: [claim-lookup-table-possible, argument-lookup-table-objection]
responds_to: [argument-bounded-table-rejoinder]
uses_concept: [concept-lookup-table-mind, concept-grain]
created: 2026-06-04
---

inference form: deductive — from the unrealisability of flat storage to the organisation of any
realisable matcher.

1. (claim-lookup-table-physically-unrealisable) A flat ~`v^n`-row table cannot be physically built —
   there is nowhere to *store* that many responses.
2. (claim-lookup-table-size-exponential) Any representation smaller than ~`v^n` must *compute* which
   response a given input maps to, rather than retrieve a pre-stored whole — and computing is
   processing over internal organisation, not lookup.
∴ (claim-realisable-behaviour-matcher-is-organised) Every physically realisable system that matches
   the behaviour is an organised computer. "Mere storage, no organisation" — the defining property of
   concept-lookup-table-mind — is not a kind anything can instantiate at mind-matching scale.

bearing — a rejoinder *to* the bounded table: this `responds_to` argument-bounded-table-rejoinder.
Grant Block his lifetime-bounded table that *covers* the inputs (claim-lifetime-bounded-table-covers-
encountered-inputs); it still fails as a counterexample, because any realisation of it is an organised
computer, not the no-organisation system the objection requires. So it `attacks`
claim-lookup-table-possible (the separability of behaviour from organisation) on its realisable
reading and undercuts argument-lookup-table-objection's transfer to actual systems: the only real
"blockheads" are organised computers (e.g. LLMs), which organisational functionalism may credit with
mind (concept-grain).

what it does NOT do: it leaves the conditional claim-lookup-table-lacks-mind standing — IF something
ran by pure stored lookup it would lack mind. The point is that nothing realisably does, at this
scale. So this is distinct from the *scale*-based deflation in argument-intuition-domain-asymmetry:
that bounds the warrant of the no-mind *intuition*; this denies the no-organisation *object* is
realisable at all. It is the standalone home of the "storage isn't a stable kind" reply that argument
records only as a weakest-premise note.

weakest premise: (2)'s claim that the addressing/retrieval organisation is *mind-relevant* rather than
mere plumbing. Block may insist a retrieval mechanism, however vast, is not cognitive organisation.
Reply: with nothing to retrieve from (premise 1), the "retrieval" is computation; the
plumbing/computation distinction has no purchase once storage is impossible. Whether that fully
answers Block is the live crux of this whole sub-web.

Distinct from argument-lookup-table-physical-impossibility (which establishes premise 1 and stops at
"unbuildable"): this draws the further, positive conclusion about the *kind* of the realisable
alternative.
